# pnpm 配置文件 - 优化安装速度和超时设置
# 使用国内镜像源加速下载
registry=https://registry.npmmirror.com

# 增加超时时间到 5 分钟（300000 毫秒）
fetch-timeout=300000

# 增加重试次数到 5 次
fetch-retries=5

# pnpm 并发连接数（平衡速度和稳定性）
network-concurrency=5

# 禁用进度条，减少输出时间
progress=false

# 只显示错误日志
loglevel=error

# pnpm 配置
# 启用严格的对等依赖检查
strict-peer-dependencies=false
# 自动安装对等依赖
auto-install-peers=true
# 使用冻结的 lockfile（CI/CD 环境）
frozen-lockfile=false

