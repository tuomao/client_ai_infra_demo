# npm 配置文件 - 优化安装速度和超时设置
# 使用国内镜像源加速下载
registry=https://registry.npmmirror.com

# 增加超时时间到 5 分钟（300000 毫秒）
fetch-timeout=300000

# 增加重试次数到 5 次
fetch-retries=5

# 限制并发连接数，避免过多并发导致超时
maxsockets=1

# 禁用进度条，减少输出时间
progress=false

# 只显示错误日志
loglevel=error

